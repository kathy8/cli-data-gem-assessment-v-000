RSpec.describe Las::Vegas::Best::Restaurants do
  it "has a version number" do
    expect(Las::Vegas::Best::Restaurants::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
